{
  "name": "beanfrog-bot",
  "version": "0.0.0",
  "main": "app.js",
  "engines": {
    "node": ">=17.7.1"
  },
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -fdr dist && tsc --build --clean",
    "start": "pnpm run build && node dist/app.js"
  },
  "dependencies": {
    "@discordjs/builders": "^0.8.2",
    "@discordjs/rest": "^0.1.0-canary.0",
    "discord-api-types": "^0.29.0",
    "discord.js": "^13.6.0"
  },
  "devDependencies": {
    "@tsconfig/node17": "^1.0.0",
    "@typescript-eslint/eslint-plugin": "^5.15.0",
    "@typescript-eslint/parser": "^5.15.0",
    "eslint": "^8.11.0",
    "prettier": "^2.5.1",
    "prettier-eslint": "^13.0.0",
    "typescript": "^4.6.2"
  }
}